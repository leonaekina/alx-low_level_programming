#include "main.h"

/**
* _isupper - check if alphabet is upper/lower
*
* @c: input character
*
* Return: 1 when true and 0 when false
*/

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
